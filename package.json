{
  "name": "iphone-tasks-pwa",
  "private": true,
  "version": "15.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check app-state.js && node --check app-ui.js && node --check app-services.js && node --check core.js && node --check sw.js && npm test"
  }
}
